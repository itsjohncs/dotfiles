# I'm unsure of the implications of --no-rehash. Pulled from
# https://github.com/pyenv/pyenv/issues/784#issuecomment-826444110. If I have
# weird issues with pyenv in the future, that'll be a good start.
eval "$(pyenv init --path --no-rehash)"
