# Dotfiles

## Linting

Run linters with pre-commit via uvx:

```bash
uvx pre-commit run --files <files>
```

Or run on all files:

```bash
uvx pre-commit run --all-files
```

Hooks: shellcheck, shfmt, ruff, prettier
